# docker-compose.yml
services:
  redis:
    image: redis:7-alpine
    command: ["redis-server","--appendonly","yes","--requirepass","devpass"]
    ports: ["6379:6379"]
    volumes:
      - redis_data:/data
volumes:
  redis_data: